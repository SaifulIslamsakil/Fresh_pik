import { Box, Container, Typography } from "@mui/material";
import SectionTitle from "../../Shred/SectionTitle";

const PropularBrand = () => {
    return (
        <Container>
            <SectionTitle
            hadding="our Propular Brand"
            ></SectionTitle>
            <Box>

            </Box>
        </Container>
    );
};

export default PropularBrand;